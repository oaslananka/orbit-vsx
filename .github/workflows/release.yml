name: Release

on:
  push:
    tags:
      - 'v*'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  attestations: write
  contents: write
  id-token: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: '24'

      - name: Verify version matches git tag
        shell: bash
        run: |
          set -euo pipefail
          tag_version="${GITHUB_REF_NAME#v}"
          pkg_version="$(jq -r '.version' package.json)"
          if [ "$tag_version" != "$pkg_version" ]; then
            echo "::error::Git tag version (${tag_version}) does not match package.json version (${pkg_version})"
            exit 1
          fi
          echo "Version check passed: ${tag_version} matches package.json"

      - name: Enable pnpm
        shell: bash
        run: |
          corepack enable
          corepack prepare pnpm@11.3.0 --activate
          pnpm --version

      - run: pnpm install --frozen-lockfile

      - run: pnpm run typecheck

      - run: pnpm run build:prod

      - name: Package VSIX
        run: pnpm exec vsce package --no-dependencies

      - name: Run packaged extension smoke test
        run: xvfb-run -a pnpm run smoke:package

      - name: Generate SBOM
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610
        with:
          path: .
          format: spdx-json
          output-file: orbit-vsx.spdx.json
          upload-artifact: false
          upload-release-assets: false

      - name: Generate checksums
        run: sha256sum ./*.vsix orbit-vsx.spdx.json > SHA256SUMS.txt

      - name: Attest release artifacts
        uses: actions/attest-build-provenance@a2bbfa25375fe432b6a289bc6b6cd05ecd0c4c32
        with:
          subject-path: |
            ./*.vsix
            orbit-vsx.spdx.json
            SHA256SUMS.txt

      - name: Publish to VS Code Marketplace
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
        run: pnpm exec vsce publish --no-dependencies --packagePath ./*.vsix

      - name: Publish to OpenVSX
        env:
          OVSX_PAT: ${{ secrets.OVSX_PAT }}
        run: pnpm exec ovsx publish ./*.vsix --pat "$OVSX_PAT"

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "$GITHUB_REF_NAME" ./*.vsix orbit-vsx.spdx.json SHA256SUMS.txt --generate-notes --verify-tag
